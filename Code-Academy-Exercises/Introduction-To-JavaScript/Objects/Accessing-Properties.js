let spaceship = {
  homePlanet: 'Earth',
  color: 'silver',
  'Fuel Type': 'Turbo Fuel',
  numCrew: 5,
  flightPath: ['Venus', 'Mars', 'Saturn']
};

// Write your code below
var crewCount = spaceship.numCrew;
var planetArray = spaceship.flightPath;
// to access an objects property With property dot notation, we write the object's name, followed by the dot operator and then the property name (key):
